export const Dashboard = ({ length_artists }) => {
    return (
        <>
            <h1 className="title">The Artist Finder</h1>
            <h4 className="title-label">Find your favorite artists and export them into a CSV-file</h4>
        </>
    )
}